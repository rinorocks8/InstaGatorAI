name: ci

on:
  push:
  pull_request:
    branches:
      - "**"

jobs:
  tests:
    name: End-to-end tests
    runs-on: ubuntu-latest
    steps:
      - run: cd .. && npm install --include=dev
      - run: cd .. && npm run test
